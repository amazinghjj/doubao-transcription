<!-- 文件用途：说明环境要求、启动参数、远程访问及跨系统数据迁移。 -->
# 通用部署指南

[返回项目首页](README.md)


## Docker Compose 部署（推荐）

### 首次启动

Linux 使用 Docker Engine 和 Compose 插件，macOS 使用支持 Compose 的 Docker 环境（如 Docker Desktop）。命令均在项目根目录执行，宿主机不需要 Node.js、Python 或 FFmpeg。

```bash
docker compose up -d --build
docker compose ps
```

首次会下载镜像、安装构建依赖并编译精简 FFmpeg，耗时比日常启动长；后续复用构建缓存。健康状态变为 `healthy` 后访问 `http://服务设备的局域网IP:8765`。默认允许局域网访问。若提示端口占用，请先停止此前的原生服务，或按下方说明换端口。

只有一个应用容器，使用 Waitress 单进程、4 线程承载 Flask。运行身份为普通用户 UID/GID 10001。配置和历史放在 `asr_data` 命名卷，挂载到 `/app/data`；不把真实配置打进镜像。

### 端口与运行管理

默认对所有宿主机 IPv4 网卡发布 8765。可在项目根目录创建 `.env`：

```dotenv
# 文件用途：本机 Compose 部署参数，不提交 Git。
ASR_PORT=8766
ASR_BIND_IP=0.0.0.0
```

然后执行 `docker compose up -d`。浏览器对应使用 `http://服务设备IP:8766`。容器内外使用同一端口，以保持 Host/Origin 校验一致。`ASR_BIND_IP` 可指定宿主机网卡地址；容器内监听地址固定为 0.0.0.0。不要只修改端口映射的一端。

```bash
docker compose logs --tail=100 -f  # 退出日志查看用 Ctrl+C，服务继续运行
docker compose stop              # 暂停
docker compose start             # 恢复
docker compose down              # 移除容器和网络，保留数据卷
```

默认 `restart: unless-stopped`，Docker 服务恢复后会恢复未被手动停止的容器。健康检查用于显示运行状态，并不会自动重启仅仅处于 unhealthy 状态的容器。日志轮转最多保留 3 份，每份 10 MB。

### 更新代码

```bash
git pull --ff-only
docker compose up -d --build
```

数据卷会保留。不要使用 `docker compose down -v`，其中 `-v` 会删除持久化数据。升级前先备份，服务重启后刷新网页以取得新请求令牌。Compose 项目名固定为 `doubao-transcription`，因此中文项目目录也能直接运行，默认卷名为 `doubao-transcription_asr_data`。如使用 `-p` 覆盖项目名，后续管理时需保持一致，否则会使用另一个数据卷。

### 备份与恢复

在没有正在进行的上传或编辑时暂停服务，备份完整数据卷：

```bash
umask 077                        # 备份包含密钥，只允许当前用户读取
docker compose stop app
docker compose run --rm --no-deps --entrypoint tar app -C /app/data -czf - . > asr-data-backup.tar.gz
docker compose start app
```

备份包含密钥和历史，请自行妥善保存，不要上传仓库。恢复到新部署的数据卷（先构建镜像，目标应为空卷；已有数据先备份，恢复会覆盖同名文件）：

```bash
docker compose build
docker compose stop app
docker compose run --rm -T --no-deps --entrypoint sh app -c 'tar -xzf - -C /app/data && chmod 700 /app/data' < asr-data-backup.tar.gz
docker compose up -d
```

从现有原生部署迁移：先停止原生服务，然后在原项目目录执行 `tar -C data -czf asr-data-backup.tar.gz .`，再按上述步骤恢复到 Docker 卷。原生 `ASR_DATA_DIR` 另有设置时，将 `data` 换成实际目录。请保留原始备份，确认网页配置和历史完整后再处理旧部署。

### 镜像体积与维护

镜像分为前端构建、Python 依赖编译、音频工具编译、最终运行四个阶段。最终阶段不包含 Node.js、npm、前端 node_modules、C 编译器、FFmpeg 源码或构建缓存。构建上下文使用白名单，排除 data、.env、.git、本机依赖及 dist；网页由源码在镜像构建时生成。

FFmpeg 使用官方 8.1.2 源码并校验 SHA256，关闭网络和视频功能，仅保留 M4A 中 AAC/ALAC、原始 AAC 及 WAV 输入和 16 kHz PCM WAV 输出所需模块。MP3/WAV/OGG 上传本来就不需要转换。非常规 M4A 编码可能不受精简版支持，遇到提示时可先转换为 MP3/WAV。源码版本、校验和及完整构建参数在 Dockerfile 中，许可证保留在镜像 `/usr/share/licenses/ffmpeg/`；升级音频库时需一起更新版本和校验和并复测。

Debian 12 x86_64 实测运行镜像约 74 MB（Docker 统计的未压缩大小），音频 FFmpeg 约 1.8 MB。其他架构或基础镜像更新后体积可能变化。构建缓存占用的是宿主机 Docker 磁盘，不属于最终镜像大小。音频上传和转换使用容器临时磁盘，请为最大约 500 MB 文件及转换结果预留足够空间，并按并发数量增加余量。数据持久化仍只有一个卷，无需数据库容器。

以下原生安装方式继续保留，便于 IDE 开发和直接运行。

## 原生部署环境要求

Linux 或 macOS，Python 3.10+（包含 venv、pip），Bash。处理 M4A/AAC 需要安装 FFmpeg，并确保 `ffmpeg -version` 可执行。

网页已编译在 dist/client，运行不需要 Node.js。需要联网访问 Python 软件源、火山语音服务和 TOS。

## 启动

解压安装包，进入工具目录：

```bash
bash start.sh
```

首次自动创建 .venv 并安装依赖。访问 http://127.0.0.1:8765 。按 Ctrl+C 停止。

如缺少 Python、venv、pip 或 FFmpeg，使用目标系统的软件包管理工具安装后重试。脚本不会修改系统全局 Python 环境。

## 配置与数据

- data/config.json：API Key、TOS AK/SK、桶名、地域，明文保存，写入权限 600。
- data/history/：历史任务和转写结果。
- ASR_DATA_DIR：可指定独立数据目录，运行用户需有读写权限。
- ASR_HOST：监听地址，默认 0.0.0.0，允许局域网 IPv4 访问，也可指定本机的内网 IPv4。
- ASR_PORT：服务端口，默认 8765。
- ASR_OPEN_BROWSER=1：可选，启动后尝试打开系统默认浏览器；无桌面环境不要设置。

例如：

```bash
ASR_DATA_DIR=/path/to/asr-data ASR_PORT=8765 bash start.sh
```

## 跨系统迁移

复制工具和你自己的 data 目录，或将 ASR_DATA_DIR 指向迁移后的数据目录。不要复制 .venv、node_modules、缓存等依赖产物；在新系统运行 start.sh 重建环境。确认 FFmpeg 位于新系统的 PATH 中。

安装包不包含个人配置或历史，这些数据需单独迁移；保留 config.json 的 600 权限。

## 局域网设备访问

在运行服务的设备上执行（Linux 与 macOS 相同）：

```bash
bash start.sh
```

等价于 `ASR_HOST=0.0.0.0 bash start.sh`。其他设备在浏览器输入 `http://服务设备的局域网IP:8765`，例如服务设备 IP 为 `192.168.6.114` 时，访问 `http://192.168.6.114:8765`。不要输入 `0.0.0.0` 或其他设备自己的 `127.0.0.1`。

也可只监听指定网卡：`ASR_HOST=192.168.6.114 bash start.sh`（该 IP 必须属于运行服务的设备）。自定义端口示例：`ASR_PORT=8766 bash start.sh`。

IP 可在系统网络设置或路由器设备列表查看。两台设备需网络互通；若打不开，请检查服务是否正在运行、IP 和端口是否正确、主机防火墙是否允许内网访问该 TCP 端口，以及 Wi-Fi 是否启用了访客/设备隔离。修改启动方式前先在旧服务终端按 Ctrl+C 停止，再启动。默认已允许局域网访问，无需额外参数。

此模式没有登录或用户隔离：访问者共用 API Key、TOS 配置和全部历史，可以查看密钥、修改配置、提交计费任务。配置和历史保存在服务设备，下载文件保存在操作浏览器的设备。HTTP 不加密，仅用于可信内网，不要进行公网端口转发。需要跨不可信网络访问时使用下方 SSH 隧道。

仅支持内网 IPv4 地址（10/8、172.16/12、192.168/16）访问，不接受任意域名和 IPv6。POST 仍校验当前页面同源及请求令牌。HTTP 页面兼容任务编号生成和文字复制；浏览器禁止自动复制时可手动选择文字或下载 TXT。

## 远程服务器

服务默认允许局域网访问，且没有公网账户认证。跨不可信网络时可通过 SSH 隧道访问：

```bash
ssh -L 8765:127.0.0.1:8765 用户名@服务器地址
```

在自己的电脑浏览器访问 http://127.0.0.1:8765 。隧道本地端口请与服务端端口一致，以满足本机请求校验。

## 开发修改

需要 Node.js 22.13+ 与 npm。在目标系统执行 `npm ci` 安装对应平台依赖，再执行 `npm run build`。Python 后端位于 local-server，网页源代码位于 app。

## 验证范围

Docker Compose 已在 Debian 12 x86_64 上实测构建、运行、三种音频转换、容器重建、卷备份恢复和局域网访问；约 74 MB。macOS Docker Desktop 与 ARM64 容器尚未实测，详见验证说明。

已在 macOS 及 Debian 12 x86_64 主机完成本地验证。Linux 使用 Python 3.11.2、Node.js 22.23.0 和 FFmpeg 8.1.1，已通过全新 Python 环境安装、源码构建、页面及接口访问、配置与历史重启恢复和音频转换。云端接口使用模拟响应，真实账户转写仍需单独测试。其他发行版和架构需按目标环境验收。

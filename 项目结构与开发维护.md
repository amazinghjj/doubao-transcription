<!-- 文件用途：说明目录职责、接口约定、开发步骤、测试边界与发布流程。 -->
# 项目结构与开发维护

[返回项目首页](README.md)


本文描述当前实现，供 IDE 阅读、功能修改、故障排查和发布维护使用。面向使用者的说明见 [使用说明.md](使用说明.md)，安装和迁移见 [部署指南.md](部署指南.md)。

## 1. 项目边界与架构

用 VS Code 等 IDE 打开包含 `package.json` 和 `start.sh` 的“录音转文字”目录。

当前采用一个项目目录：源码、编译产物、依赖和个人数据位于不同子目录，尚未拆分为独立源码仓库与运行目录。

```text
浏览器
  ├─ 加载网页 ─────────────── Flask → dist/client/
  └─ /api/* ──────────────── Flask → local-server/
                                ├─ settings.py → data/config.json
                                ├─ history.py  → data/history/*.json
                                ├─ FFmpeg     → 临时 WAV 文件
                                ├─ TOS SDK    → 用户的私有存储桶
                                └─ requests   → 火山录音识别 submit / query
```

- 网页：React、TypeScript，使用 Vinext/Vite 编译，静态导出。
- 本地服务：Python、Flask，源码直接运行，不需要编译为可执行文件。
- 云端识别：固定使用 `volc.seedasr.auc`，通过提交任务、轮询结果实现。
- 持久化：本地 JSON 文件；没有数据库或云端应用存储。
- 目标系统：Linux、macOS，同一启动脚本；音频转换仅通过 PATH 中的 FFmpeg。

## 2. 目录职责

```text
录音转文字/
├── app/
│   ├── page.tsx                 主页面与交互流程
│   ├── layout.tsx               页面框架、标题、图标和元信息
│   └── globals.css              主题、布局和组件样式
├── components/ui/               当前使用的五个基础界面组件
├── lib/                         前端辅助函数
├── public/
│   ├── favicon.svg              站点图标
│   └── 使用说明.html            网页版操作说明，构建时复制到产物
├── local-server/
│   ├── server.py                HTTP 路由、上传、转写、访问校验
│   ├── settings.py              配置校验、读取与原子写入
│   ├── history.py               任务记录与转写结果持久化
│   └── requirements.txt         Python 直接依赖版本
├── dist/
│   ├── client/                  实际部署使用的静态网页
│   └── server/                  构建工具生成的服务端产物，本地运行不使用
├── data/                        个人数据，禁止随公开安装包发布
│   ├── config.json              API Key、AK/SK、桶名、地域
│   └── history/*.json           每个任务一份历史记录
├── .venv/                       当前机器的 Python 环境，不跨系统复制
├── node_modules/                前端开发依赖，不跨系统复制
├── .next/                       框架缓存或构建辅助产物
├── tsconfig.tsbuildinfo          TypeScript 增量检查缓存
├── start.sh                     两个系统共用的运行入口
├── package.json                 前端依赖与脚本
├── package-lock.json            npm 依赖锁文件
├── vite.config.ts               Vinext、样式处理和开发代理配置
├── next.config.ts               静态导出配置 output: export
├── tsconfig.json                TypeScript 配置
├── next-env.d.ts                框架类型声明
├── .oxlintrc.json / .oxfmtrc.json 检查及格式化规则
└── *.md                         使用、部署、维护及验证文档
```

不要直接编辑 `dist/`：下次编译会覆盖。`public/` 的资源修改也需要重新构建，才能反映到由 Flask 提供的网页中。

## 3. 建议阅读顺序

1. `start.sh`：了解启动和依赖安装方式。
2. `local-server/server.py`：了解静态网页如何提供、请求如何转发。
3. `app/page.tsx`：了解配置加载、上传、轮询、历史和下载交互。
4. `local-server/settings.py`、`history.py`：了解保存语义和数据结构。
5. `app/globals.css`：调整视觉与响应式布局。

主页面目前集中在一个文件中，包含 `SecretField`、配置表单、转写流程、结果编辑和历史列表。后续功能显著增多时，可按这些职责拆分；当前没有独立的前端业务服务层。

## 4. 主要接口

除静态资源外，前端通过同源 `/api/` 调用本地服务。

| 方法与路径 | 职责 |
|---|---|
| GET `/api/session` | 获取本次服务进程的本机请求令牌及应用标识 |
| POST `/api/settings/read` | 读取配置，回填当前使用的值 |
| POST `/api/settings/save` | 保存或更新配置，返回保存后的配置 |
| POST `/api/upload` | 接收本地文件及 TOS 配置，转换并上传，返回临时下载链接 |
| POST `/api/submit` | 提交音频链接、任务编号和识别选项，保存任务状态 |
| POST `/api/query` | 用原任务编号查询结果，更新本地历史 |
| POST `/api/history/list` | 返回历史摘要，不携带全部分句 |
| POST `/api/history/read` | 读取指定任务和结果 |
| POST `/api/history/edit` | 保存编辑后的文字 |

本地服务检查 Host；POST 需要 `X-Local-Token`，存在 Origin 时还会检查来源。这是回环地址工具的请求防护，不是适用于公网的登录系统。服务重启后令牌变化，旧页面需要刷新。

配置读取接口按产品要求返回真实密钥以便编辑；API Key 和 SK 由密码输入框默认遮挡，AK 明文显示。不要在日志、截图或测试输出中打印接口里的真实凭据。

## 5. 数据与状态约定

### 配置

`settings.py` 只保存 `key`、`ak`、`sk`、`bucket`、`region` 五个字段。空值表示保留已有配置，并不删除已有密钥。

配置是明文 JSON，使用临时文件加 `os.replace` 原子替换，文件权限为 600。损坏的 JSON 会报错，不会静默覆盖。默认存储目录为项目内 `data/`，可用 `ASR_DATA_DIR` 指向其他位置。

### 历史

以 UUID 为文件名。记录包含名称、创建/更新时间、状态、原始识别结果及可编辑文字。

状态主要为 `submitting`、`submitted`、`queued`、`running`、`done`、`interrupted`。`interrupted` 表示需要继续查询，不能直接等同于云端识别失败。

重复查询已完成任务不会覆盖用户修改过的文字。TXT 导出使用编辑后的文字；SRT、时间戳文本及原始 JSON 使用云端原始结果。

浏览器 localStorage 只保留最近任务编号；配置与历史的权威数据在服务端文件中。

### 音频

本地临时文件在请求结束时清理。云端音频仍保留在用户 TOS 的 `local-asr/` 前缀下；生命周期由用户配置。不要在只改历史记录的操作中附带删除云端音频。

## 6. 修改与运行

所有命令均在项目根目录执行。

### 安装与启动

```bash
bash start.sh
```

脚本检查 Python 版本，必要时创建 `.venv` 并安装依赖，然后运行 Flask。默认地址为 `http://127.0.0.1:8765`。

注意：脚本目前通过 import 检查依赖是否已安装，不会自动判断已安装版本是否与更新后的 requirements 完全一致。修改 Python 依赖后应显式执行：

```bash
.venv/bin/python -m pip install -r local-server/requirements.txt
```

### 修改网页

需要 Node.js 22.13+：

```bash
npm ci
./node_modules/.bin/tsc --noEmit
npm run build
```

保持本地服务运行，构建结束后刷新网页即可。不要在构建尚未结束时刷新，以免加载到未完成的静态产物。

### 修改后端

编辑 `local-server/` 后，停止原服务并重新执行 `bash start.sh`。目前没有开启自动重载。

### 现有脚本的适用范围

- `npm run build`：实际使用的网页构建命令。
- `npm run lint` / `npm run format`：代码检查/格式化；大范围格式化会产生较多变更。
- `npm run dev`：网页开发服务器。当前 `/api` 代理指向 8765，但后端 Origin 校验只允许服务自身地址，不能直接当作已配置完成的端到端开发环境。可靠流程仍为构建后访问 Flask 地址；如要使用热更新，需要单独设计并限制开发来源。
- `npm start`：调用 `bash start.sh`，与通用运行入口保持一致。

已移除未使用的组件、移动端 hook、组件生成配置和托管工具直接依赖。保留 `shadcn` 是因为全局样式仍引用其 CSS；保留 React/RSC 构建插件是因为 Vinext 构建需要它们。

## 7. 测试与排查

当前已执行的检查范围见 [验证说明.md](验证说明.md)。此前的自动化测试脚本在本次工作区外层 `work/` 下，不在项目包内，也尚未配置 `npm test` 或项目内统一测试命令。接手长期维护时应将可复用测试整理进项目 `tests/`，用临时数据目录和模拟凭据运行，避免触及真实配置。

每次修改按影响验证：

- 前端：类型检查、构建；涉及交互时检查保存、回填、显示切换及错误提示。
- 配置：保存后重新打开仍能读取，空密钥保留原值，写入失败不破坏旧文件。
- 历史：查询结果能持久保存，重启可读，重复查询不覆盖修改。
- 转写：只提交一次，后续轮询同一个 UUID；网络超时先恢复查询。
- 跨系统：使用新环境重建依赖，验证 FFmpeg 位于 PATH，以及数据目录可写。

定位问题优先检查本地 HTTP 状态和页面错误。向火山排查时使用返回的状态码及日志编号，不提供密钥。

2026-09-08 已在 Debian 12 x86_64 主机实测部署包安装、源码构建、配置与历史重启恢复、FFmpeg 转换及本地访问校验。云端 TOS/识别采用模拟响应，未执行真实云端转写；详细环境和测试边界见验证说明。

## 8. 发布与迁移

当前通用 ZIP 同时包含源码与运行产物，不是“纯运行包”。打包前完成类型检查与编译，再整理以下内容：

- 包含源码、配置模板/构建配置、依赖清单及锁文件、`public/`、文档、`start.sh`、`dist/client/`。
- 排除 `data/`、`.venv/`、`node_modules/`、缓存、`__pycache__/`、`.pyc`、`.tsbuildinfo`、`dist/server/` 及版本控制内部文件。
- 保留启动脚本权限，检查 ZIP 可完整解压且入口文件存在。
- 发布包不得携带任何真实密钥、签名音频链接、个人历史或录音。

个人迁移时另行复制 `data/`，并在目标环境重建依赖；公共发布与个人迁移是两种不同的数据范围。

目前打包由维护流程执行，仓库内尚无专门的发布脚本。后续自动化时应把以上排除规则固化，并为安装包做内容检查。

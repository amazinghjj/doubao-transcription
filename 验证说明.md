<!-- 文件用途：记录当前验证范围、检查结果及尚未完成的验证。 -->
# 当前验证范围

[返回项目首页](README.md)


配置保存、重新读取、修改、失败保护、权限及历史记录测试通过。配置输入框回填当前值，敏感字段默认遮挡。

前端类型检查与静态编译通过，通用启动脚本通过语法检查。音频转换统一使用 PATH 中的 FFmpeg；代码不依赖特定系统的目录或系统框架。

当前已完成 macOS 和 Debian 12 x86_64 本地验证，Linux 实测详情见下文。云端模拟测试不代表所有账户的实际权限和额度均有效。

## 未使用文件清理验证（2026-09-08）

删除 55 个未引用组件、关联移动端 hook 和组件生成配置，共 57 个文件；移除 12 项未使用的直接依赖，并更新依赖锁文件。五个实际使用的组件和业务源码保持不变。

- 依据页面入口递归检查组件引用，清理后不存在断开的本地导入。
- 使用 npm ci 重装依赖后，TypeScript 检查和静态构建通过。
- 13 项后端和配置持久化回归测试通过。
- npm start 已改为调用通用启动脚本，并在独立端口与测试数据目录实际启动验证。
- 页面、静态资源、使用说明、配置读取、历史列表接口检查通过。
- 个人数据和保留的业务源码哈希未改变，文档相对链接检查通过。
- 本次未调用收费云端转写，也未新增 Linux 主机实测。

## Linux 主机实测（2026-09-08）

测试代码版本：`2f3203a`。使用独立测试目录、虚拟环境和合成测试数据，没有迁移个人配置、录音或历史。

| 项目 | 结果 |
|---|---|
| 主机 | Debian GNU/Linux 12，x86_64 |
| Python | 3.11.2，新建 venv，按 requirements.txt 安装成功；pip check 通过 |
| FFmpeg | 8.1.1，实际生成 AAC/M4A 并经项目上传处理路径转换为 WAV 成功 |
| Node.js | 22.23.0，官方发行包 SHA-256 校验通过，独立目录安装 |
| 前端 | npm ci、TypeScript 类型检查、npm run build 通过 |
| 后端回归 | 9 项接口/历史/转写流程测试通过 |
| 配置存储 | 4 项真实临时文件测试通过 |
| HTTP 运行 | 原部署包与 Linux 新构建产物均能启动，页面、资源和使用说明正常访问 |
| 重启恢复 | 真实服务停止并重启后，配置值和历史编辑内容仍保留 |
| 本地访问保护 | 旧令牌失效、异常 Origin 被拒绝、数据文件不通过静态路由暴露 |
| 写入权限 | config.json 为 600 |

主机访问 npm 软件源时出现 ECONNRESET。使用已有 npm 下载缓存辅助重试后完成安装；Linux 依赖由 npm 根据锁文件重新安装，没有复制 Mac 的 node_modules 或 Python 虚拟环境。此项为软件源连接条件，不是修改代码解决的问题。

真实 HTTP 测试覆盖启动、页面资源、配置持久化、权限、历史编辑持久化、重启令牌、配置修改、私有文件路由限制、来源检查。测试结束已停止测试端口上的服务；未修改既有业务容器或全局 Node.js 配置。

边界：TOS 上传和识别服务采用模拟响应；FFmpeg 转换、文件持久化、进程重启和静态构建为真实执行。本次没有使用真实密钥、没有上传个人录音、没有调用收费识别。未执行浏览器点击交互测试，也不代表其他 Linux 发行版、架构均已验证。

## 局域网访问验证（2026-09-08）

默认监听 `0.0.0.0`，无需 `--lan` 即可局域网访问，并保留 `ASR_HOST` 配置。TypeScript 检查、前端构建及原有 13 项后端测试通过；新增局域网测试覆盖监听模式、内网 IPv4 Host、错误端口/域名、同源 Origin、请求令牌、配置保存读取、600 权限、历史列表与私有文件路由。

真实 HTTP 服务使用独立临时数据目录和端口 18767，通过 Mac 内网地址 192.168.6.110 访问；另从 Debian 主机 192.168.6.114 经局域网请求页面，返回 HTTP 200。测试未使用个人密钥或调用计费接口，结束后已停止测试服务。此次不是在 Linux 上重新部署新版本，而是从 Linux 验证跨设备访问。

前端兼容测试在禁用 randomUUID 与 Clipboard API 的模拟环境中验证 UUID v4 格式、1000 次生成不重复及传统复制回退。未逐一验证手机浏览器实际复制权限；失败时页面仍提示手动复制。

默认启动回归：清除 ASR_HOST 环境变量后，直接执行 `bash start.sh`（不带参数），从 Linux 主机访问 Mac 测试端口返回 HTTP 200；局域网测试 2 项及原有后端测试 13 项通过。

## Docker Compose 验证（2026-09-08）

在 Debian 12 x86_64 主机，使用 Docker 28.5.2 / Compose 5.1.3 完成源码构建及真实容器运行。独立测试项目 `doubao-compose-validation` 使用 18768 端口，未修改已有业务容器。

- 最终镜像为 Python 3.12 Alpine + Waitress 3.0.2 + 静态网页 + 精简 FFmpeg 8.1.2，大小 73,851,236 字节（约 74 MB，未压缩）；初版完整 FFmpeg 镜像为 200,118,368 字节。
- FFmpeg 二进制约 1.8 MB；最终容器不含 node/npm/gcc/cc、前端依赖或构建源码。运行 UID/GID 为 10001，Python 依赖检查通过。
- 通过实际上传函数，验证合成 AAC M4A、ALAC M4A、原始 AAC 均转换为 16 kHz、16 bit PCM WAV；TOS 调用被模拟，临时文件清理通过。
- 从 Mac 访问 Linux 容器的页面及 JS/CSS 资源、配置保存回填、历史编辑、Host/Origin/令牌拒绝逻辑、私有文件路由均通过。
- 删除容器后重建，配置与历史保留，旧请求令牌失效；文档中的数据卷备份及普通用户恢复流程实测通过，配置权限保持 600。
- Compose 默认项目名及默认端口 8765 解析通过，实机 HTTP 使用自定义端口 18768，验证端口映射与请求校验一致。

本次没有执行真实账户的付费转写，也没有在 macOS Docker Desktop / ARM64 上构建运行容器；Dockerfile 未固定 CPU 架构，其他平台需在对应环境验收。测试数据均为合成数据，测试结束停止并移除独立测试容器，保留测试卷和构建缓存。
